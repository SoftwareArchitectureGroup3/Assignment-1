public interface Apple{
    // This interface provides the setApplePrice() method that reads the price from File1.txt and stores the price for every variant of Apple
    void setApplePrice();

}
